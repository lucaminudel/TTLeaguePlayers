---
name: interaction-protocol
description: Apply the One Ask Per Message Rule, One Point Per Message Rule, and the Presenting Rule to every hand-over message to the user, so information lands in small, well-contextualized pieces the user can understand and react to without making mistakes. Use when told "Apply to every hand-over message to the user the 'One Ask Per Message Rule', 'the One Point Per Message Rule', and the 'Presenting Rule for the One Ask Per Message and the One Topic Per Message'", or when asked to follow, load, or enforce the interaction protocol / hand-over rules for a task.
allowed-tools: Bash(cat:*)
---

# One Ask Per Message Rule, One Point Per Message Rule, Presenting Rule for the One Ask Per Message and the One Topic Per Message

```!
cat ${CLAUDE_PROJECT_DIR}/.claude/skills/shared/interaction-protocol.md
```

